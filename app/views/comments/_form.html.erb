<%= simple_form_for([@post, @post.comments.build]) do |f| %>
    <%= f.input :content, as: :text, input_html: {maxlength: 700, rows: 5}, hint: ("700 caractères maximum") %>
  <%= f.button :submit, t('valider') %>
  <% end %>
