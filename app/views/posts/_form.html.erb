<%= simple_form_for @post, :html => { :multipart => true } do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.input :author %>
  <%= f.file_field :post_img %>
  <%= select_tag(:category_id, options_for_select(@categories), :prompt => "Choisir la matière") %>
  <%= f.button :submit %>
<% end %>
