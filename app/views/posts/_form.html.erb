<%= simple_form_for @post do |f| %>
  <%= f.input :title, input_html: {maxlength: 50}%>
  <%= f.input :content, as: :text, input_html: {maxlength: 99999999, rows: 14} %>
  <%= f.input :author %>
  <%= f.file_field :post_img %>
  <%= select_tag(:category_id, options_for_select(@categories), :prompt => "Choisir la matière" ) %>
  <%= f.button :submit %>
<% end %>
