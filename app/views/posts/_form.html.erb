<%= simple_form_for @post do |f| %>
  <%= f.input :title, input_html: {maxlength: 30}%>
  <%= f.input :content, input_html: {maxlength: 99999999} %>
  <%= f.input :author %>
  <%= f.file_field :post_img %>
  <%= select_tag(:category_id, options_for_select(@categories), :prompt => "Choisir la matière" ) %>
  <%= f.button :submit %>
<% end %>
